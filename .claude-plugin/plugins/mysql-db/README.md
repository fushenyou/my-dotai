# MySQL DB Plugin

> MySQL 数据库智能助手 - 提供数据库结构分析、SQL 生成、数据可视化和架构设计功能

## 概述

MySQL DB 插件是一个功能强大的数据库助手，集成了 `@f4ww4z/mcp-mysql-server` MCP 服务器，为 Claude Code 提供了完整的 MySQL 数据库操作能力。

通过智能的技能和命令，你可以：
- 🔍 **分析数据库结构** - 自动缓存表结构、索引和关系
- ⚡ **生成优化 SQL** - 基于结构生成高性能查询
- 📊 **数据可视化** - 使用 ECharts 生成交互式图表
- 🏗️ **架构设计** - 设计合理、高性能的表结构

## 功能特性

### 1. 数据库结构分析 (`/mysql-db:schema`)

- 自动获取所有表结构
- 分析索引和外键关系
- 识别表之间的关联
- 缓存到模型记忆
- 支持导出为 JSON 文件

### 2. SQL 智能生成 (`/mysql-db:gen`)

- 基于缓存结构生成 SQL
- 自动优化 JOIN 和索引
- 提供多种实现方案
- 输出执行计划分析
- 给出性能优化建议

### 3. 数据分析与可视化 (`/mysql-db:analysis`)

- 执行数据分析查询
- 识别趋势和模式
- 生成 ECharts 可视化
- 提供业务洞察
- 输出完整的 HTML 报告

### 4. 数据库架构设计 (`/mysql-db:design`)

- 扮演 DB 架构师角色
- 设计规范化的表结构
- 优化索引策略
- 考虑扩展性
- 输出完整的 DDL 语句

## 安装

### 前置要求

1. MySQL 数据库（5.7+ 或 8.0+）
2. Node.js 和 npm（用于 MCP 服务器）

### 配置 MCP 服务器

项目配置：
项目目录下：.claude/settings.json
```json
{
  "env": {
    "MYSQL_HOST": "your_host",
    "MYSQL_USER": "your_user",
    "MYSQL_PASSWORD": "your_password",
    "MYSQL_DATABASE": "your_database"
  }
}
```

在 Claude Code 配置文件中添加 MySQL MCP 服务器：

**macOS/Linux**: `~/.claude/settings.json`
**Windows**: `%APPDATA%\Claude\settings.json`

```json
{
  "mcpServers": {
    "mysql": {
      "command": "npx",
      "args": ["-y", "@f4ww4z/mcp-mysql-server"],
      "env": {
        "MYSQL_HOST": "localhost",
        "MYSQL_USER": "your_username",
        "MYSQL_PASSWORD": "your_password",
        "MYSQL_DATABASE": "your_database",
        "MYSQL_PORT": "3306"
      }
    }
  }
}
```

或者使用连接字符串：

```json
{
  "mcpServers": {
    "mysql": {
      "command": "npx",
      "args": [
        "-y",
        "@f4ww4z/mcp-mysql-server",
        "mysql://your_username:your_password@localhost:3306/your_database"
      ]
    }
  }
}
```

### 启用插件

插件会自动被发现和加载。重启 Claude Code 后即可使用。

## 使用方法

### 1. 分析数据库结构

```bash
# 基本使用
/mysql-db:schema

# 保存到文件
/mysql-db:schema --out ./db-schema
```

**输出示例**：
```
✅ 数据库结构分析完成

**数据库**: my_database
**表数量**: 15
**分析时间**: 2024-01-23 10:30:00

**主要表**:
- users (用户表) - 12 字段
- orders (订单表) - 15 字段
- products (商品表) - 18 字段

✅ 结构已缓存到记忆中
```

### 2. 生成 SQL 查询

```bash
# 查询用户订单
/mysql-db:gen 查询最近30天的订单，包括用户信息和订单详情

# 统计分析
/mysql-db:gen 按月统计销售额，显示趋势

# 多表关联
/mysql-db:gen 查询购买次数最多的前10名用户
```

**输出示例**：
```markdown
✅ SQL 查询语句已生成

**SQL 语句**:
```sql
SELECT
  o.id AS order_id,
  o.order_no,
  o.total_amount,
  u.username,
  COUNT(oi.id) AS item_count
FROM orders o
INNER JOIN users u ON o.user_id = u.id
LEFT JOIN order_items oi ON o.id = oi.order_id
WHERE o.created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY o.id
ORDER BY o.created_at DESC
LIMIT 50;
```

**性能建议**:
✅ orders.created_at 有索引，查询高效
⚠️ 建议为 orders.status 添加索引
```

### 3. 数据分析与可视化

```bash
# 销售趋势分析
/mysql-db:analysis 分析最近12个月的销售趋势

# 用户行为分析
/mysql-db:analysis 分析用户的注册和活跃情况

# 产品分析
/mysql-db:analysis 分析各类别产品的销售占比

# 自定义输出
/mysql-db:analysis 按月统计订单金额 --out ./reports/sales-trend.html
```

**输出示例**：
```markdown
✅ 数据分析完成

**数据摘要**:
- 总订单数: 15,847
- 总金额: ¥2,345,678.90
- 平均订单金额: ¥148.02

**关键发现**:
1. 📈 整体呈上升趋势，月均增长 8.5%
2. 🔥 11-12月为销售旺季
3. 💡 复购用户占 42%，贡献 68% 的销售额

📄 已生成 HTML 文件: ./reports/analysis-20240123.html
```

### 4. 数据库架构设计

```bash
# 电商系统设计
/mysql-db:design 设计一个电商系统的数据库，包括用户、商品、订单、分类等功能

# 内容管理系统
/mysql-db:design 设计一个 CMS 系统，支持文章、分类、标签、评论

# 带参数的设计
/mysql-db:design 设计一个在线教育平台 --tables 20 --records 100000 --qps 1000
```

**输出示例**：
```markdown
✅ 数据库设计完成

## 实体关系图
```
users ||--o{ orders : 下单
orders ||--|{ order_items : 包含
products ||--o{ order_items : 被订购
```

## 表结构设计
### users - 用户表
[详细的表结构...]

## DDL 语句
[完整的建表 SQL...]
```

## 工作流程

### 推荐使用顺序

1. **第一步：分析结构**
   ```bash
   /mysql-db:schema
   ```
   缓存数据库结构到记忆中

2. **第二步：生成 SQL**
   ```bash
   /mysql-db:gen 查询需求描述
   ```
   基于结构生成优化的 SQL

3. **第三步：数据分析**
   ```bash
   /mysql-db:analysis 分析需求描述
   ```
   生成可视化报告

4. **按需设计**
   ```bash
   /mysql-db:design 设计需求描述
   ```
   设计新的表结构

## 技能说明

插件包含 4 个核心技能，可以自动触发或通过命令调用：

### schema-analysis
数据库结构分析技能，用于获取并缓存数据库结构信息。

**何时使用**：
- 需要了解数据库的完整结构
- 需要分析表之间的关系和依赖
- 为 SQL 生成提供上下文

### sql-generation
SQL 生成优化技能，基于数据库结构生成高性能查询。

**何时使用**：
- 需要生成复杂的查询语句
- 需要多表关联查询
- 需要优化现有查询性能

### data-visualization
数据分析可视化技能，生成交互式图表。

**何时使用**：
- 需要可视化数据分析结果
- 需要生成数据报表
- 需要发现数据趋势和模式

### database-design
数据库架构设计技能，设计合理的表结构。

**何时使用**：
- 需要设计新的数据库表结构
- 需要重构现有数据库
- 需要评估数据库设计方案

## 配置说明

### 环境变量

在 MCP 服务器配置中使用以下环境变量：

| 变量 | 说明 | 示例 |
|------|------|------|
| MYSQL_HOST | 数据库主机 | localhost |
| MYSQL_PORT | 数据库端口 | 3306 |
| MYSQL_USER | 数据库用户名 | root |
| MYSQL_PASSWORD | 数据库密码 | password |
| MYSQL_DATABASE | 数据库名称 | mydb |

### 连接字符串

也可以使用 MySQL 连接字符串格式：

```
mysql://[user[:password]]@host[:port][/database]
```

示例：
```
mysql://root:password@localhost:3306/mydb
```

## 最佳实践

### 1. 定期更新结构缓存

当数据库结构发生变化时，重新运行：
```bash
/mysql-db:schema
```

### 2. 先分析后生成

在使用 SQL 生成前，确保已缓存数据库结构：
```bash
/mysql-db:schema
/mysql-db:gen 查询需求
```

### 3. 验证生成的 SQL

生成的 SQL 在执行前应：
- 使用 `EXPLAIN` 查看执行计划
- 在测试环境验证
- 检查性能影响

### 4. 数据可视化采样

对于大数据集，先采样再可视化：
```sql
SELECT * FROM large_table
TABLESAMPLE SYSTEM(10);  -- 采样 10%
```

### 5. 设计考虑扩展性

设计表结构时考虑：
- 预估数据量
- 查询模式
- 分表分库策略
- 索引优化

## 常见问题

### 1. 连接失败

**问题**：无法连接到数据库

**解决方案**：
- 检查数据库服务是否运行
- 验证连接参数是否正确
- 确认网络连接和防火墙设置
- 检查用户权限

### 2. 权限不足

**问题**：某些表无法查询

**解决方案**：
- 确保数据库用户有足够的权限
- 授予 SELECT 权限：`GRANT SELECT ON database.* TO 'user'@'host';`
- 刷新权限：`FLUSH PRIVILEGES;`

### 3. 性能问题

**问题**：查询速度慢

**解决方案**：
- 使用 `EXPLAIN` 分析查询计划
- 为常用字段添加索引
- 优化 JOIN 顺序
- 考虑分区或分表

### 4. 结构缓存过期

**问题**：SQL 基于旧结构生成

**解决方案**：
```bash
# 重新分析结构
/mysql-db:schema --out ./new-schema
```

## 示例场景

### 场景 1：电商数据分析

```bash
# 1. 分析结构
/mysql-db:schema

# 2. 查询订单数据
/mysql-db:gen 查询最近30天的销售数据，包括用户、商品、金额

# 3. 生成可视化报告
/mysql-db:analysis 按月统计销售额和订单量，生成趋势图 --out ./reports/sales.html

# 4. 优化查询
/mysql-db:gen 优化上面的查询，添加索引建议
```

### 场景 2：数据库重构

```bash
# 1. 设计新结构
/mysql-db:design 设计用户订单系统的表结构，支持百万级数据

# 2. 生成 DDL
# 输出包含完整的建表语句

# 3. 验证设计
# 检查输出的设计说明和建议

# 4. 迁移数据
/mysql-db:gen 生成数据迁移脚本，从旧表迁移到新表
```

### 场景 3：性能优化

```bash
# 1. 分析慢查询
/mysql-db:gen 分析这个慢查询：SELECT * FROM orders WHERE status = 1 AND created_at > '2024-01-01'

# 2. 生成优化建议
# 插件会提供索引建议和查询优化

# 3. 验证效果
# 使用 EXPLAIN 对比优化前后的执行计划
```

## 技术栈

- **MCP 服务器**: [@f4ww4z/mcp-mysql-server](https://github.com/f4ww4z/mcp-mysql-server)
- **可视化**: [ECharts](https://echarts.apache.org/)
- **数据库**: MySQL 5.7+, 8.0+
- **运行时**: Node.js

## 项目结构

```
.claude-plugin/plugins/mysql-db/
├── .claude-plugin/
│   └── plugin.json          # 插件配置
├── commands/                # 命令文件
│   ├── mysql-db-schema.md
│   ├── mysql-db-gen.md
│   ├── mysql-db-analysis.md
│   └── mysql-db-design.md
├── skills/                  # 技能文件
│   ├── schema-analysis/
│   │   └── SKILL.md
│   ├── sql-generation/
│   │   └── SKILL.md
│   ├── data-visualization/
│   │   └── SKILL.md
│   └── database-design/
│       └── SKILL.md
└── README.md               # 本文档
```

## 开发

### 本地测试

1. 克隆项目
2. 配置 MySQL MCP 服务器
3. 重启 Claude Code
4. 测试各个命令

### 调试

使用调试模式启动 Claude Code：
```bash
claude --debug
```

查看插件加载信息和日志。

## 贡献

欢迎贡献！你可以：
- 报告 Bug
- 提出新功能建议
- 提交 Pull Request
- 改进文档

## 许可证

MIT License

## 作者

Created with ❤️ for Claude Code community

## 相关链接

- [mcp-mysql-server](https://github.com/f4ww4z/mcp-mysql-server) - MySQL MCP 服务器
- [Claude Code 文档](https://docs.anthropic.com/claude-code)
- [MySQL 文档](https://dev.mysql.com/doc/)

## 更新日志

### v0.1.0 (2024-01-23)
- ✨ 初始版本
- ✅ 支持数据库结构分析
- ✅ 支持智能 SQL 生成
- ✅ 支持数据可视化
- ✅ 支持数据库架构设计
- ✅ 集成 mcp-mysql-server
